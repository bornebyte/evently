"use client";
/* eslint-disable @next/next/no-img-element */

import Image from "next/image";
import Link from "next/link";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { EventCard } from "@/components/event-card";
import { ArrowLeft, ArrowRight, ArrowUpRight, CalendarDays, ChevronDown, Clock3, CreditCard, Heart, MapPin, Mail, Minus, Plus, QrCode, Share2, ShieldCheck, Users } from "@/components/icons";
import { SiteFooter } from "@/components/site-footer";
import { SiteHeader } from "@/components/site-header";
import { events, formatINR, type EventItem } from "@/lib/events";

type BuyerDetails = { fullName: string; email: string; phone: string; paymentReference: string };

export default function EventDetails({ event }: { event: EventItem }) {
  const [tierIndex, setTierIndex] = useState(0);
  const [quantity, setQuantity] = useState(1);
  const [saved, setSaved] = useState(false);
  const [checkoutOpen, setCheckoutOpen] = useState(false);
  const [submitted, setSubmitted] = useState(false);
  const [error, setError] = useState("");
  const [bookingReference, setBookingReference] = useState("");
  const [buyer, setBuyer] = useState<BuyerDetails>({ fullName: "", email: "", phone: "", paymentReference: "" });
  const tier = event.ticketTiers[tierIndex];
  const total = useMemo(() => tier.price * quantity, [quantity, tier.price]);
  const related = events.filter((item) => item.slug !== event.slug && item.category === event.category).slice(0, 2);

  const submitBooking = async (formEvent: FormEvent<HTMLFormElement>) => {
    formEvent.preventDefault();
    if (!buyer.fullName || !buyer.email || !buyer.phone || !buyer.paymentReference) {
      setError("Please complete every field so the payment can be verified.");
      return;
    }
    setError("");
    try {
      const response = await fetch("/api/bookings", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ eventSlug: event.slug, ticketName: tier.name, quantity, ...buyer }) });
      const result = await response.json() as { error?: string; booking?: { reference: string; status: string; total: number } };
      if (!response.ok || !result.booking) {
        setError(result.error ?? "We could not submit this booking. Please try again.");
        return;
      }
      setBookingReference(result.booking.reference);
      window.localStorage.setItem("evently-booking", JSON.stringify({ reference: result.booking.reference, event: event.slug, buyer, ticket: tier.name, quantity, total: result.booking.total, status: "PENDING_REVIEW" }));
      setSubmitted(true);
      setCheckoutOpen(false);
    } catch {
      setError("We could not reach the booking service. Please check your connection and try again.");
    }
  };

  return (
    <main className="min-h-screen bg-[#f7f7f4] text-[#242725]">
      <div className="border-b border-[#e8eae5]"><SiteHeader /></div>
      <div className="mx-auto max-w-[1280px] px-5 pb-20 pt-7 lg:px-8 lg:pt-9">
        <div className="mb-7 flex items-center gap-3 text-[11px] font-medium text-[#8a938c]"><Link className="inline-flex items-center gap-1.5 hover:text-[#242725]" href="/events"><ArrowLeft size={14} /> All events</Link><span>/</span><span>{event.category}</span><span>/</span><span className="truncate text-[#242725]">{event.title}</span></div>
        <div className="grid gap-10 lg:grid-cols-[1.15fr_0.85fr] lg:items-start lg:gap-14">
          <div className="reveal">
            <div className="image-zoom relative aspect-[1.2/0.82] overflow-hidden rounded-[26px] bg-[#202321] shadow-[7px_8px_0_#e5e1db]"><Image alt={event.title} className="object-cover" fill priority sizes="(max-width: 1024px) 92vw, 60vw" src={event.image} /><div className="absolute inset-0 bg-gradient-to-t from-black/45 via-transparent to-transparent" /><div className="absolute left-5 top-5 flex items-center gap-2 rounded-full bg-white/90 px-3 py-2 text-[10px] font-bold uppercase tracking-[0.14em]"><span className="h-1.5 w-1.5 rounded-full bg-[#f16d55]" /> {event.category}</div><div className="absolute bottom-5 left-5 flex h-16 w-16 flex-col items-center justify-center rounded-2xl bg-[#f16d55] text-[#242725]"><span className="text-[10px] font-bold tracking-[0.14em]">{event.month}</span><span className="font-display text-[30px] leading-7">{event.day}</span></div></div>
            <div className="mt-8 flex flex-wrap items-start justify-between gap-5"><div><p className="mb-2 text-[12px] font-bold uppercase tracking-[0.16em] text-[#e15f49]">{event.tag ?? "Featured experience"}</p><h1 className="font-display max-w-[650px] text-[clamp(3rem,5.5vw,5.4rem)] leading-[0.88]">{event.title}</h1></div><div className="flex gap-2"><button aria-label={saved ? "Unsave event" : "Save event"} className={`flex h-11 w-11 items-center justify-center rounded-full border ${saved ? "border-[#f16d55] bg-[#f16d55]" : "border-[#dfe3dd] bg-white hover:border-[#f16d55]"}`} onClick={() => setSaved(!saved)} type="button"><Heart size={17} /></button><button aria-label="Share event" className="flex h-11 w-11 items-center justify-center rounded-full border border-[#dfe3dd] bg-white hover:border-[#f16d55]" onClick={() => navigator.clipboard?.writeText(window.location.href)} type="button"><Share2 size={17} /></button></div></div>
            <p className="mt-5 max-w-[620px] text-[16px] leading-7 text-[#6f7871]">{event.description}</p>
            <div className="mt-7 grid max-w-[620px] gap-3 sm:grid-cols-2"><div className="flex gap-3 rounded-2xl border border-[#e4e6e0] bg-white p-4"><CalendarDays className="mt-0.5 shrink-0 text-[#e15f49]" size={18} /><div><p className="text-[12px] font-semibold">{event.dateLong}</p><p className="mt-1 text-[11px] text-[#879088]">Add to calendar</p></div></div><div className="flex gap-3 rounded-2xl border border-[#e4e6e0] bg-white p-4"><Clock3 className="mt-0.5 shrink-0 text-[#e15f49]" size={18} /><div><p className="text-[12px] font-semibold">{event.time}</p><p className="mt-1 text-[11px] text-[#879088]">Doors open 30 min early</p></div></div><div className="flex gap-3 rounded-2xl border border-[#e4e6e0] bg-white p-4 sm:col-span-2"><MapPin className="mt-0.5 shrink-0 text-[#e15f49]" size={18} /><div><p className="text-[12px] font-semibold">{event.location}</p><p className="mt-1 text-[11px] text-[#879088]">{event.city} · <a className="underline underline-offset-2 hover:text-[#e15f49]" href="#venue">View map</a></p></div></div></div>
          </div>

          <aside className="reveal reveal-delay-2 lg:sticky lg:top-6">
            {submitted ? <PendingConfirmation event={event} reference={bookingReference} tier={tier.name} quantity={quantity} total={total} /> : checkoutOpen ? <PaymentForm buyer={buyer} error={error} eventSlug={event.slug} onBack={() => { setCheckoutOpen(false); setError(""); }} onChange={setBuyer} onSubmit={submitBooking} total={total} /> : <TicketSelector event={event} quantity={quantity} setQuantity={setQuantity} setTierIndex={setTierIndex} tier={tier} tierIndex={tierIndex} total={total} onContinue={() => setCheckoutOpen(true)} />}
            {!submitted && !checkoutOpen && <div className="mt-4 flex items-center gap-3 rounded-2xl border border-[#e4e6e0] bg-[#eef3eb] p-4"><div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-[#d4e2cf] text-[#5c7755]"><Users size={15} /></div><p className="text-[11px] leading-4 text-[#64705f]"><span className="font-semibold text-[#424c40]">{event.attendees}</span> are already making plans to be here.</p></div>}
          </aside>
        </div>

        <div className="mt-20 grid gap-12 border-t border-[#e4e6e0] pt-14 lg:grid-cols-[1fr_0.72fr]"><div><p className="mb-3 text-[11px] font-bold uppercase tracking-[0.17em] text-[#e15f49]">The details</p><h2 className="font-display text-[clamp(2.5rem,4vw,3.7rem)] leading-[0.92]">Come for the<br /><span className="text-[#9aa29b]">whole story.</span></h2><div className="mt-9 divide-y divide-[#e4e6e0]"><DetailRow label="What to expect">A thoughtful day filled with talks, hands-on sessions and the kind of conversations that carry on long after the last slide.</DetailRow><DetailRow label="Good to know">All tickets include lunch, tea and coffee. The venue is fully accessible. Bring an open mind and a notebook if that’s your thing.</DetailRow><DetailRow label="The line-up">Mina Patel · Founder, Studio Common<br />Jon Bell · Creative director, New Ritual<br />Sasha Okafor · Artist and educator</DetailRow></div></div><div id="venue"><div className="rounded-[22px] bg-[#e8e3ef] p-6 sm:p-7"><div className="flex items-start justify-between"><div><p className="text-[11px] font-bold uppercase tracking-[0.15em] text-[#72647c]">Your destination</p><h3 className="mt-2 text-[23px] font-semibold tracking-[-0.05em]">{event.location}</h3></div><span className="flex h-10 w-10 items-center justify-center rounded-full bg-white/60"><MapPin size={17} /></span></div><div className="relative mt-8 h-44 overflow-hidden rounded-[16px] bg-[#c9c4d1]" style={{ backgroundImage: "radial-gradient(#a39baa 1px, transparent 1px), linear-gradient(125deg, transparent 45%, #b3adb9 46%, #b3adb9 47%, transparent 48%), linear-gradient(10deg, transparent 48%, #a9a3b0 49%, #a9a3b0 51%, transparent 52%)", backgroundSize: "13px 13px, 100% 100%, 100% 100%" }}><span className="absolute left-[56%] top-[42%] flex h-10 w-10 items-center justify-center rounded-full bg-[#f16d55] text-[#242725] shadow-lg"><MapPin size={18} /></span><span className="absolute bottom-3 left-3 rounded-full bg-white/80 px-3 py-1.5 text-[10px] font-semibold backdrop-blur">{event.city}</span></div><p className="mt-4 text-[12px] leading-5 text-[#6f6679]">Chalk Farm Road, NW1 8EH<br />A short walk from Chalk Farm station.</p><a className="mt-5 inline-flex items-center gap-2 text-[12px] font-semibold text-[#242725] hover:text-[#e15f49]" href="#venue">Get directions <ArrowUpRight size={14} /></a></div><div className="mt-4 rounded-[22px] border border-[#e4e6e0] bg-white p-6"><p className="text-[11px] font-bold uppercase tracking-[0.15em] text-[#919a93]">Hosted by</p><div className="mt-4 flex items-center gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-full bg-[#202321] font-display text-xl text-[#f16d55]">F</span><div><p className="text-[13px] font-semibold">{event.organizer}</p><p className="text-[11px] text-[#8b948d]">{event.organizerRole}</p></div></div></div></div></div>
      </div>
      {related.length > 0 && <section className="border-t border-[#e4e6e0] bg-white"><div className="mx-auto max-w-[1280px] px-5 py-16 lg:px-8"><div className="flex items-end justify-between gap-4"><div><p className="mb-3 text-[11px] font-bold uppercase tracking-[0.17em] text-[#e15f49]">Keep exploring</p><h2 className="font-display text-[clamp(2.4rem,4vw,3.7rem)] leading-[0.92]">More like this.</h2></div><Link className="hidden items-center gap-2 text-[12px] font-semibold hover:text-[#e15f49] sm:flex" href="/events">See all events <ArrowRight size={15} /></Link></div><div className="mt-9 grid gap-5 md:grid-cols-2">{related.map((item) => <EventCard event={item} key={item.slug} />)}</div></div></section>}
      <SiteFooter />
    </main>
  );
}

function TicketSelector({ event, tier, tierIndex, setTierIndex, quantity, setQuantity, total, onContinue }: { event: EventItem; tier: EventItem["ticketTiers"][number]; tierIndex: number; setTierIndex: (value: number) => void; quantity: number; setQuantity: (value: number) => void; total: number; onContinue: () => void }) {
  return <div className="rounded-[24px] border border-[#e1e4df] bg-white p-5 shadow-[0_15px_35px_rgba(36,39,37,0.08)] sm:p-6"><div className="flex items-center justify-between border-b border-[#edf0eb] pb-5"><div><p className="text-[11px] font-bold uppercase tracking-[0.16em] text-[#879088]">Tickets from</p><p className="mt-1 text-[28px] font-semibold tracking-[-0.06em]">{formatINR(event.price)}</p></div><span className="rounded-full bg-[#f7ded7] px-3 py-2 text-[11px] font-semibold text-[#b84e3c]">{event.capacity - event.sold} spots left</span></div><div className="mt-5"><label className="mb-2 block text-[11px] font-bold uppercase tracking-[0.13em] text-[#879088]" htmlFor="ticket-tier">Choose a ticket</label><div className="relative"><select className="w-full appearance-none rounded-xl border border-[#dfe3dd] bg-[#fafbf9] px-4 py-3.5 pr-10 text-[13px] font-semibold outline-none focus:border-[#f16d55]" id="ticket-tier" onChange={(e) => setTierIndex(Number(e.target.value))} value={tierIndex}>{event.ticketTiers.map((item, index) => <option key={item.name} value={index}>{item.name} · {formatINR(item.price)}</option>)}</select><ChevronDown className="pointer-events-none absolute right-4 top-1/2 -translate-y-1/2" size={16} /></div><p className="mt-2 text-[11px] text-[#89928b]">{tier.description} · {tier.remaining} remaining</p></div><div className="mt-5 flex items-center justify-between rounded-xl bg-[#f7f7f4] px-4 py-3"><span className="text-[12px] font-semibold">Quantity</span><div className="flex items-center gap-3"><button aria-label="Decrease quantity" className="flex h-7 w-7 items-center justify-center rounded-full border border-[#d9ded8] bg-white hover:border-[#f16d55] disabled:opacity-40" disabled={quantity === 1} onClick={() => setQuantity(Math.max(1, quantity - 1))} type="button"><Minus size={13} /></button><span className="w-4 text-center text-[13px] font-semibold">{quantity}</span><button aria-label="Increase quantity" className="flex h-7 w-7 items-center justify-center rounded-full border border-[#d9ded8] bg-white hover:border-[#f16d55] disabled:opacity-40" disabled={quantity >= 8 || quantity >= tier.remaining} onClick={() => setQuantity(Math.min(8, quantity + 1))} type="button"><Plus size={13} /></button></div></div><div className="mt-5 flex items-center justify-between border-t border-[#edf0eb] pt-5 text-[13px]"><span className="text-[#7b847d]">Total</span><span className="text-[19px] font-semibold">{formatINR(total)}</span></div><button className="pulse-soft mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-[#f16d55] px-5 py-4 text-[13px] font-bold text-[#242725] hover:bg-[#242725] hover:text-white" onClick={onContinue} type="button"><CreditCard size={17} /> Continue to payment</button><div className="mt-4 flex items-center justify-center gap-1.5 text-[10px] text-[#8c958e]"><ShieldCheck size={13} /> Manual payment verification · No hidden fees</div></div>;
}

function PaymentForm({ buyer, onChange, onSubmit, onBack, total, error, eventSlug }: { buyer: BuyerDetails; onChange: (buyer: BuyerDetails) => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void; onBack: () => void; total: number; error: string; eventSlug: string }) {
  const paymentUpi = process.env.NEXT_PUBLIC_PAYMENT_UPI ?? "evently@upi";
  const [paymentQr, setPaymentQr] = useState<{ label: string; payload: string; imageUrl: string | null } | null>(null);
  useEffect(() => { fetch(`/api/payment-qr/${eventSlug}`).then(async (response) => { if (response.ok) { const result = await response.json() as { qr: { label: string; payload: string; imageUrl: string | null } | null }; setPaymentQr(result.qr); } }).catch(() => undefined); }, [eventSlug]);
  return <div className="rounded-[24px] border border-[#e1e4df] bg-white p-5 shadow-[0_15px_35px_rgba(36,39,37,0.08)] sm:p-6"><button className="mb-5 flex items-center gap-1 text-[11px] font-semibold text-[#7a847c] hover:text-[#242725]" onClick={onBack} type="button"><ArrowLeft size={13} /> Back to tickets</button><div className="border-b border-[#edf0eb] pb-5"><div className="flex items-center justify-between"><div><p className="text-[11px] font-bold uppercase tracking-[0.16em] text-[#e15f49]">Step 2 of 2</p><h2 className="mt-2 text-[23px] font-semibold tracking-[-0.05em]">Complete your booking</h2></div><span className="flex h-10 w-10 items-center justify-center rounded-full bg-[#e8e3ef] text-[#6f5d7d]"><QrCode size={19} /></span></div><p className="mt-3 text-[12px] leading-5 text-[#7b847d]">Pay using the QR below, then share the payment reference so our team can verify it.</p></div><div className="mt-5 flex items-center gap-4 rounded-2xl bg-[#f7f7f4] p-4">{paymentQr?.imageUrl ? <img alt="Payment QR code" className="h-[82px] w-[82px] rounded-lg object-cover" src={paymentQr.imageUrl} /> : <div className="grid h-[82px] w-[82px] shrink-0 grid-cols-5 grid-rows-5 gap-1 border-4 border-[#242725] bg-white p-2">{Array.from({ length: 25 }).map((_, index) => <span className={(index * 7 + index % 3) % 4 !== 0 ? "bg-[#242725]" : "bg-transparent"} key={index} />)}</div>}<div><p className="text-[10px] font-bold uppercase tracking-[0.13em] text-[#929b94]">Scan & pay</p><p className="mt-1 text-[13px] font-semibold">{paymentQr?.label ?? "Evently payments"}</p><p className="mt-1 break-all font-mono text-[11px] text-[#6e786f]">{paymentQr?.payload ?? paymentUpi}</p><p className="mt-1 text-[11px] text-[#879088]">Amount due: <span className="font-semibold text-[#242725]">{formatINR(total)}</span></p></div></div><form className="mt-5 space-y-3" onSubmit={onSubmit}><InputField label="Full name" placeholder="Your full name" type="text" value={buyer.fullName} onChange={(value) => onChange({ ...buyer, fullName: value })} /><div className="grid gap-3 sm:grid-cols-2"><InputField label="Email address" placeholder="you@example.com" type="email" value={buyer.email} onChange={(value) => onChange({ ...buyer, email: value })} /><InputField label="Mobile number" placeholder="+91 98765 43210" type="tel" value={buyer.phone} onChange={(value) => onChange({ ...buyer, phone: value })} /></div><InputField label="Payment reference / UTR" placeholder="Enter the reference from your payment" type="text" value={buyer.paymentReference} onChange={(value) => onChange({ ...buyer, paymentReference: value })} />{error && <p className="rounded-lg bg-[#fbe5e0] px-3 py-2 text-[11px] font-medium text-[#bb503e]">{error}</p>}<button className="mt-2 flex w-full items-center justify-center gap-2 rounded-xl bg-[#242725] px-5 py-4 text-[13px] font-bold text-white hover:bg-[#f16d55] hover:text-[#242725]" type="submit">Submit payment for review <ArrowUpRight size={15} /></button></form><p className="mt-4 text-center text-[10px] leading-4 text-[#929b94]">By submitting, you agree that your details may be used to verify this booking and send your ticket.</p></div>;
}

function InputField({ label, placeholder, type, value, onChange }: { label: string; placeholder: string; type: string; value: string; onChange: (value: string) => void }) { return <label className="block"><span className="mb-1.5 block text-[10px] font-bold uppercase tracking-[0.1em] text-[#879088]">{label}</span><input className="w-full rounded-xl border border-[#dfe3dd] bg-[#fafbf9] px-3 py-3 text-[12px] outline-none placeholder:text-[#a5ada6] focus:border-[#f16d55]" onChange={(event) => onChange(event.target.value)} placeholder={placeholder} required type={type} value={value} /></label>; }

function PendingConfirmation({ event, reference, tier, quantity, total }: { event: EventItem; reference: string; tier: string; quantity: number; total: number }) { return <div className="rounded-[24px] border border-[#ead8ae] bg-[#fff8e9] p-6 shadow-[0_15px_35px_rgba(36,39,37,0.08)]"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-[#f0cb75] text-[#6c5728]"><Clock3 size={24} /></div><p className="mt-6 text-[11px] font-bold uppercase tracking-[0.16em] text-[#a0782e]">Payment submitted</p><h2 className="mt-2 font-display text-[38px] leading-[0.9]">You’re almost in.</h2><p className="mt-4 text-[13px] leading-5 text-[#796d55]">Our team will verify your payment and send your confirmed ticket link, PDF ticket and receipt to your email within 1 hour.</p><div className="mt-6 rounded-2xl border border-[#ead8ae] bg-white/70 p-4"><div className="flex items-center justify-between text-[11px] text-[#8d7b58]"><span>Booking reference</span><span className="font-mono font-semibold text-[#242725]">{reference}</span></div><div className="mt-3 flex items-center justify-between border-t border-[#ead8ae] pt-3 text-[12px]"><span>{quantity} × {tier}</span><span className="font-semibold">{formatINR(total)}</span></div></div><div className="mt-5 flex items-start gap-2 text-[11px] leading-4 text-[#806f4e]"><Mail className="mt-0.5 shrink-0" size={14} /> Watch your inbox for the approval email and unique ticket link.</div><Link className="mt-5 flex items-center justify-center gap-2 rounded-full bg-[#242725] px-5 py-3.5 text-[13px] font-semibold text-white hover:bg-[#f16d55] hover:text-[#242725]" href="/tickets">Track my booking <ArrowRight size={15} /></Link><p className="mt-4 text-center text-[10px] text-[#9b8a69]">{event.title}</p></div>; }

function DetailRow({ label, children }: { label: string; children: React.ReactNode }) { return <div className="grid gap-3 py-5 sm:grid-cols-[145px_1fr]"><p className="text-[11px] font-bold uppercase tracking-[0.14em] text-[#919a93]">{label}</p><p className="text-[13px] leading-6 text-[#6e776f]">{children}</p></div>; }
